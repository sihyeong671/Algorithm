//특정한 최단 경로

#include <iostream>
#include <vector>

using namespace std;

int N, E, v1, v2;

int main(){
  cin >> N >> E;
  int a, b ,c;
  for(int i = 0; i < N; ++i){
    cin >> a >> b >> c;
  }
  cin >> v1 >> v2;
  return 0;
}